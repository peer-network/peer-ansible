# ==================================================
# FILE: roles/backend/meta/main.yml
# Backend role dependencies (follows bash script order)
# ==================================================
---
dependencies:
  - role: common          # STEP 1
  - role: apache2 
  - role: php           # STEP 2, 3, 4, 7
  - role: rust_and_cargo  # STEP 5
  - role: www_permissions # STEP 8
